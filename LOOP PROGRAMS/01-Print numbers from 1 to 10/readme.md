Logic

1. Initialize i = 1.

2. while(i ≤ 10) → loop runs as long as i is ≤ 10.

3. Inside the loop:

- Prints the current value of i.

- Increments i by 1.

4. When i becomes 11 → condition fails → loop stops.