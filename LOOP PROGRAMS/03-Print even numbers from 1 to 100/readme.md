Logic

1. Loop variable i runs from 1 to 100.

2. Inside the loop:

- if (i % 2 == 0) checks if the number is divisible by 2.

- If true, the number is even, so it gets printed.

3. The else part is empty because no action is needed for odd numbers.