Logic

1. The loop variable i runs from 1 to 100.

2. Inside the loop:

- if (i % 2 != 0) checks if the number gives a non-zero remainder when divided by 2.

- If true, it’s an odd number and gets printed.

3. The else part is empty as even numbers are skipped.