Logic

1. The user enters an integer.

2. The program finds the remainder when dividing the number by 2 using the modulus operator (%).

- If remainder = 0 → Even

- If remainder = 1 or -1 → Odd

3. The result is checked using a switch-case structure.

4. The corresponding message is printed on the screen.