Logic

1. The program prompts the user to enter a single character.

2. Using a switch-case structure:

- If the character matches any alphabet (A–Z or a–z), it prints "alphabet".

- If it matches any digit (0–9), it prints "digit".

- Otherwise, it prints "special symbol".

3. The default case handles any characters not covered by the above cases.